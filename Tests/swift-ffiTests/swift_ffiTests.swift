import XCTest
@testable import swift_ffi

final class swift_ffiTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(swift_ffi().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
